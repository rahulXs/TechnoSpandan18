<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.streak.technospandan">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".TechnoSpandan"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.DaysViewActivity" />
        <activity android:name=".activity.CategoryView" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notifications_black_24dp" />

        <activity
            android:name=".activity.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.GroupView" />
        <activity android:name=".activity.AboutUsActivity" />
        <activity android:name=".activity.TechnicalSectionActivity" />
        <activity android:name=".activity.SportsSectionActivity" />
        <activity android:name=".activity.CulturalSectionActivity" />
        <activity android:name=".activity.ImgUploadActivity"></activity>
        <activity android:name=".activity.DisplayImagesActivity"></activity>
        <activity android:name=".activity.SignInActivity"></activity>
    </application>

</manifest>